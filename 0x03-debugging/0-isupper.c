#include "main_h"
/**
 * _isupper -function that check for uppercase character.
 * @c: input
 * Return: i if c is uppercase, O otherwise
 */
int _isupper(int c)
{
if ((c >= 'A') && (c <= 'Z'))

return (1);

else
return (0);
}
